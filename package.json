{
  "name": "linkedin-ai-filter",
  "version": "0.1.0",
  "description": "Custom AI Filter for LinkedIn Chrome Extension",
  "type": "module",
  "scripts": {
    "build": "node scripts/bundle-content.js",
    "test": "node scripts/bundle-content.js && node --test test/*.test.js",
    "bench": "node test/benchmark.js"
  }
}
